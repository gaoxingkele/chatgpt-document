# 网页爬虫：首选 Pyppeteer，备用 Playwright
pyppeteer>=1.0.2
playwright>=1.40.0   # 备用，运行 install-browser 安装 Chromium

# Kimi API（兼容 OpenAI SDK）
openai>=1.0.0

# Word 文档生成与读取
python-docx>=1.1.0

# PDF 文本提取
pypdf>=4.0.0

# 环境变量与配置
python-dotenv>=1.0.0

# HTTP 与工具
httpx>=0.27.0

# API 重试
tenacity>=8.2.0

# Step0b 本地预处理
scikit-learn>=1.3.0     # TF-IDF、KMeans、cosine similarity
datasketch>=1.6.0       # MinHash 近似去重
networkx>=3.0           # TextRank（PageRank 算法）
# 可选（Mode B 知识图谱）：
# spacy>=3.7.0          # NER + 依存分析
# python -m spacy download en_core_web_sm

# 可选：Claude 需 anthropic，Gemini 需 google-generativeai
# pip install anthropic google-generativeai
